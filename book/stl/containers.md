## Контейнеры 

В стандартной библиотеке с++ `STL` есть множество классов позволяющие 
работать с нескольками обьектами в одной переменной, такой обьект
называется `контейнером`.

Есть несколько типов контейнеров:

- `Последовательные контейнеры`: Данные хранатся линейное, имеется порядок.  
    - vector:

    - list:

    - deque:

    - forward_list:

    - array:

- `Адаптеры контейнеров`:Те же линейные контейнеры но с некоторыми 
ограничениями и узко-направленные.
    
    - stack:

    - queue:

    - priority_queue:


- `Ассоциативные контейнеры`: Хранит данные так чтобы можно было найти по 
ключу или по значению максимально быстро.Порядок определяет сам контейнер
а не ты. 

    - **Упорядоченные**
        
        - set: 

        - map:

        - multiset:

        - multimap:

    - **Неупорядоченные** 

        - unordered_map:

        - unordered_multiset:

        - unordered_multimap:

