## Контейнеры 

В стандартной библиотеке с++ `STL` есть множество классов позволяющие 
работать с нескольками обьектами в одной переменной, такой обьект
называется `контейнером`.


# 📦 Типы контейнеров 
---

## 🔹 Последовательные контейнеры
Хранят элементы **линейно**, в определённом порядке.

---

### `vector`
**Динамический массив**

- Элементы лежат в памяти подряд
- Быстрый доступ по индексу `O(1)`
- Медленная вставка/удаление в середине `O(n)`
- Автоматически увеличивает размер

**Используется, когда:** нужен быстрый доступ по индексу

---

### `list`
**Двусвязный список**

- Каждый элемент знает о предыдущем и следующем
- Нет быстрого доступа по индексу
- Быстрая вставка и удаление `O(1)`

**Используется, когда:** часто вставляешь/удаляешь элементы

---

### `deque`
**Двусторонняя очередь**

- Похож на `vector`
- Можно быстро добавлять в начало и конец
- Не всегда лежит подряд в памяти

**Используется, когда:** нужно работать с началом и концом

---

### `forward_list`
**Односвязный список**

- Только указатель на следующий элемент
- Меньше памяти, чем `list`
- Нет доступа назад

**Используется, когда:** нужна минимальная память

---

### `array`
**Статический массив фиксированного размера**

- Размер известен во время компиляции
- Лежит в стеке
- Быстрее `vector`

**Используется, когда:** размер не меняется

---

## 🔹 Адаптеры контейнеров
Обёртки над другими контейнерами с ограниченным интерфейсом.

---

### `stack`
**Стек (LIFO — последний вошёл, первый вышел)**

- Работает как стопка

**Используется, когда:** нужен стек

---

### `queue`
**Очередь (FIFO — первый вошёл, первый вышел)**

**Используется, когда:** нужна обычная очередь

---

### `priority_queue`
**Очередь с приоритетом**

- Самый большой элемент всегда сверху
- Реализован через кучу (heap)

**Используется, когда:** важен приоритет

---

## 🔹 Ассоциативные контейнеры
Хранят данные для **быстрого поиска по ключу**.

Контейнер сам управляет порядком.

---

## ✅ Упорядоченные (на деревьях, обычно Red-Black Tree)

> Все операции: `O(log n)`

---

### `set`
**Множество уникальных элементов**

- Все элементы уникальны
- Отсортированы

**Пример:** `{1, 3, 5, 7}`

---

### `map`
**Ключ → значение**

- Ключи уникальны
- Отсортированы

**Пример:** `{ "age" → 20 }`

---

### `multiset`
**Множество с повторениями**

- Элементы могут повторяться
- Отсортированы

---

### `multimap`
**Карта с повторяющимися ключами**

- Один ключ → несколько значений

---

## ❌ Неупорядоченные (на хеш-таблицах)

> Средняя сложность: `O(1)`

---

### `unordered_set`
**Множество без порядка**

- Быстрый доступ
- Нет сортировки

---

### `unordered_map`
**Хеш-таблица ключ → значение**

- Очень быстрый поиск
- Порядок не гарантирован

---

### `unordered_multiset`
**unordered_set с повторами**

---

### `unordered_multimap`
**unordered_map с повторяющимися ключами**

---

# 📊 Таблица сравнения

```
| Контейнер     | Доступ    | Вставка   | Удаление  | Порядок |
|---------------|-----------|-----------|-----------|---------|
| vector        | O(1)      | O(n)      | O(n)      | Да    |
| list          | O(n)      | O(1)      | O(1)      | Да    |
| deque         | O(1)      | O(1)      | O(1)      | Да    |
| set/map       | O(log n)  | O(log n)  | O(log n)  | Да    |
| unordered_map | O(1)      | O(1)      | O(1)      |Нет    |
```


---



## Итераторы


